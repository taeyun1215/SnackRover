server:
  port: 8080

spring:
  profiles:
    active: dev
  datasource:
    url: jdbc:mariadb://localhost:3306/Snack_Rover
    username: root
    password: 6548

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect